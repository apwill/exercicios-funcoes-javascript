function subtracao (num1, num2) {
    let resultadoSubtracao = num1-num2
    let mensagem = `A subtração entre o ${num1} e o ${num2} é igual a: ${resultadoSubtracao} ` 
    
    return mensagem 
}

console.log(subtracao(9, 2))